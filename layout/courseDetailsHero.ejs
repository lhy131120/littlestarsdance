<section
	id="hero"
	class="hero <%= (typeof isCoursesDetailsPage !== 'undefined' && isCoursesDetailsPage) ? 'course-details-hero' : null %>"
>
	<picture class="position-absolute top-0 start-0 w-100 h-100 z-n1">
		<img
			src="<%= (typeof heroImageSrc !== 'undefined' && heroImageSrc) ? heroImageSrc : '/assets/images/inside-heros/hero-1.jpg' %>"
			alt="hero image"
			class="w-100 h-100 object-fit-cover object-position-center"
		/>
	</picture>

	<div class="container position-relative z-3">
		<div class="py-5">
			<h1 class="mb-4 mb-lg-7 fs-3 fs-lg-1 letter-spacing-1 text-center"><%- courseName %></h1>

			<% if (typeof label !== 'undefined' && Array.isArray(label) && label.length > 0) { %>
			<div class="d-flex align-items-center justify-content-center gap-3 flex-wrap">
				<% label.forEach(function(item) { %>
				<span class="badge text-gray-900 rounded-5 bg-primary-300 px-4 py-1 fs-7 fs-lg-6 letter-spacing-1">
          <%= item %>
        </span>
				<% }); %>
			</div>
			<% } %>
		</div>
	</div>
</section>
